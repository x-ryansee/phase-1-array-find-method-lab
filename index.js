// code your solution here

function superbowlWin(games) {
    const wonGame = games.find((game) => {
        return game.result === "W" 
    
    });
    wonGames.map(game => {
        return wonGame.year;
    })
}